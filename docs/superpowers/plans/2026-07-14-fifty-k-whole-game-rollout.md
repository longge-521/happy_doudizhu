# 510K 全局决策采样实施计划

1. 在训练测试中增加蓄水池均匀性、单局样本数和位置统计断言，先确认旧实现失败。
2. 为 `SelfPlayCollection` 增加开局/中局/残局计数，并实现跨进程合并。
3. 将前 4 次决策抽样替换为全局蓄水池采样，延迟执行每局唯一一次候选动作模拟。
4. 在训练阶段日志中输出三段位置统计。
5. 更新根目录 `README.md` 的训练说明。
6. 运行定向测试、相关回归测试和 4000 局真实训练评测；根据正式评测决定是否需要下一阶段特征升级。
