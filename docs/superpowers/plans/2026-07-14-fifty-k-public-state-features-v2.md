# 510K 公开局势特征 v2 实施计划

1. 先增加失败测试，覆盖 AI 上下文公开得分/牌墩、特征座次编码和模型清单特征版本门禁。
2. 为 `AIContext` 增加带默认值的公开状态字段，并由 `build_ai_context` 从房间同步填充。
3. 扩展 `build_fifty_k_features`，保持原 7 个尾部标量顺序不变，新增 22 维公开局势信息。
4. 为训练产物增加 `features_version`，加载时拒绝旧特征清单并安全降级。
5. 更新根目录 `README.md` 的模型输入、版本和降级说明。
6. 运行定向测试及全部 510K 相关回归测试，自查 diff 与残留进程。
7. 使用相同种子运行 4000 局训练和 500 局独立评测，与 36.6%/-1.33 基线对比。
