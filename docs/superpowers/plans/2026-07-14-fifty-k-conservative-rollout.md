# 510K 保守 Rollout 训练实施计划

> **执行约束：** 当前会话使用 `executing-plans` 按测试驱动方式执行；遵守仓库 `AGENTS.md`，用户人工确认前不执行 Git 提交。

**目标：** 修复 Rollout 弱化胜局优先级、坏检查点继续训练和相同种子不可复现的问题。

**架构：** 保持动作价值模型、候选生成和评测协议不变。只在现有训练器内调整排序损失权重、检查点回滚以及随机种子初始化。

**技术栈：** Python、PyTorch、pytest。

## 全局约束

- 不修改经典与不洗牌 DouZero 路径。
- 不修改模型特征维度、权重格式和上线门槛。
- 不删除训练产物，不执行 Git 提交。

### 任务一：失败测试锁定三个根因

**文件：**

- 修改：`backend/tests/test_fifty_k_training.py`

- [x] 将“奖励尺度无关”测试改为“终局奖励差决定排序梯度强度”。
- [x] 增加坏正式检查点恢复最佳权重并重置后续训练起点测试。
- [x] 增加相同训练种子产生相同初始权重测试。
- [x] 运行三个测试，确认当前实现分别因损失相同、状态未回滚和权重不同而失败。

### 任务二：最小实现

**文件：**

- 修改：`backend/app/training/fifty_k/trainer.py`

- [x] 在创建模型前固定 PyTorch CPU/CUDA 随机种子。
- [x] 将每对 Rollout 排序损失乘以 `abs(left_target - right_target)`，按有效候选对数量取平均。
- [x] 正式检查点未提升时加载 `best_state` 并重建 Adam 优化器。
- [x] 运行新增测试和 510K 训练定向测试。

### 任务三：文档和真实验证

**文件：**

- 修改：`README.md`

- [x] 记录奖励差加权、坏检查点回滚和随机种子保证。
- [x] 运行短程真实训练并核对 Teacher、周期检查点和最终检查点日志。
- [x] 检查测试进程、语法、差异和训练产物。
